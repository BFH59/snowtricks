{% for label, messages in app.flashes %}
    <div class="flash-msg">
        <div class="container">
            {% for message in messages %}
                <div class="alert alert-{{ label }}">
                    <p>{{ message | raw }}</p>
                </div>
            {% endfor %}
        </div>
    </div>

{% endfor %}