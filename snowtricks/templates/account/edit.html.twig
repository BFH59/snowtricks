{% extends 'base.html.twig' %}

{% block title %}Modifiez votre compte !{% endblock %}

{%  block body %}
    <div class="container mt-5">
        <h1>Modifiez votre compte</h1>

        {{ form_start(form) }}

        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">Modifier mon compte</button>

        {{ form_end(form) }}
    </div>
{% endblock %}